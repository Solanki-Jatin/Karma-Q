# Karma-Q
a Java/Spring Boot distributed job scheduler where a pool of workers reliably picks up, executes, retries, and recovers scheduled or recurring background jobs, using Postgres as the coordination layer instead of a separate message broker.
